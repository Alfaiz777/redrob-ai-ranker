# ── Core ranking pipeline ─────────────────────────────────────────────────────
# The ranking engine (run.py) uses only Python stdlib + re.
# These packages are needed for analysis scripts and the dashboard only.
pydantic==2.13.4
numpy==2.4.6
pandas==3.0.3
tqdm==4.68.3

# ── Dashboard (React API server) ──────────────────────────────────────────────
fastapi==0.137.2
uvicorn==0.49.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==9.1.0

# ── Optional: FAISS retrieval pipeline (NOT used in submission) ───────────────
# Only needed if building the FAISS index for the optional two-stage pipeline.
# The standard reproduce command (python run.py) does NOT require these.
sentence-transformers==5.6.0
faiss-cpu==1.14.3
